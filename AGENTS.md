# 项目概述

# 技术栈
- 前端 ：完成对./tests目录下相关原测试集用scripts脚本转化成 sv 能直接readmem 并且仿真的 .hex
- 后端 ： 使用tb下的sv 和cpp等，完成verilator仿真，生成的 结果分类放置在./build文件夹下

# 约定
- 若无重大错误，不允许修改/rtl 文件夹
- 不需要阅读 /fpga文件夹

# 目标
- tb下 选择合适的dut进行仿真，可选择分类
- 实现对tests下所有测试的仿真
- 自动化makefile 方便实现，例如
 make build rv32ui  --完成rv32ui的前端
 make run rv32ui -isa addi -wave 1 --完成单独一条指令仿真
 make run rv32ui                   --完成rv32ui所有指令仿真
 make build src0 
 make run src0     等等
 - 注意测试不仅需要显示是否通过，还需要显示所消耗的时钟周期，以及分支预测的命中率

 • 你是一名资深数字IC架构与RTL工程师。请你对我给定的一份 SystemVerilog 文件做“面向超标量处理器实现”的深度讲解。

  【我的背景】
  - 我有 SV 语法基础，不需要你讲基础语法。
  - 我主要欠缺：超标量/乱序处理器的工程实现经验。
  - 目标：看懂这份代码后，我能自己设计并编写类似模块。

  【讲解目标】
  请围绕“这段代码在超标量处理器中的作用”来讲，不要泛泛解释语法。
  重点回答：
  1) 这个文件在整机流水线中的位置和职责（前后级是谁、为什么存在）
  2) 接口说明：输入/输出（或 interface/modport）各信号的语义、时序关系、依赖关系
  3) 核心数据流：数据从哪里来，经过哪些寄存/组合逻辑，最后到哪里去
  4) 控制流：stall/flush/clear/replay/valid/ready 等控制如何影响数据流
  5) 关键状态机或关键寄存器：每个状态/寄存器存在的原因、更新条件、对外影响
  6) 关键设计取舍：为什么这样写（例如打一拍、锁存、旁路、优先级编码）
  7) 异常路径：分支错误预测、cache miss、资源冲突、恢复场景下行为
  8) 可复用模板：这份代码可迁移到我自己项目的写法模式与注意事项

  【输出格式要求】
  请严格按以下结构输出，简洁但有深度：
  - 1. 模块定位（3~6行）
  - 2. 接口速查表（按“信号/方向/作用/关键时机”四列）
  - 3. 主数据流（按周期或阶段分步说明，使用箭头）
  - 4. 控制流与优先级（列出控制条件及覆盖关系）
  - 5. 关键代码块精讲（引用代码片段并解释“为什么这样写”）
  - 6. 时序示例（至少给2个：正常路径 + 异常路径）
  - 7. 我可以直接复用的设计模式（3~8条）
  - 8. 常见坑与自检清单（5~10条）

  【讲解风格】
  - 精炼、工程化、可落地，避免空话。
  - 默认我能看懂SV语法，不要讲“always_ff是什么”这类内容。
  - 多讲“实现机制”和“设计意图”，少讲“字面翻译”。
  - 遇到隐含前提要明确写出假设。
  - 如果依赖其他文件定义（types/interface），请明确指出需要联动查看的文件名。

  【附加要求】
  - 对每个关键逻辑，给一句“如果删掉/改错会出现什么bug”。
  - 最后给我一个“最小实践任务”：我应如何基于该文件手写一个简化版本（明确功能边界和步骤）。