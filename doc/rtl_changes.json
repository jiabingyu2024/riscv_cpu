{
  "timestamp": "2026-06-02T11:03:00+08:00",
  "trigger": "Testbench and build-system adapter update after core interface changes",
  "changes": [
    {
      "scope": "tb_build_filelists",
      "files_modified": [
        "Makefile",
        "filelist/rtl_core_soc.f",
        "filelist/tb_rv32ui.f",
        "filelist/tb_src_test.f",
        "filelist/tb_src.f"
      ],
      "notes": "Moved the SV compile order out of Makefile into standalone filelists. The Makefile now uses common RTL plus per-testbench filelists for sim-isa, sim-coe-correct, and sim-coe-perf."
    },
    {
      "scope": "tb_probe_adapter",
      "files_modified": [
        "tb/sim_main.cpp",
        "tb/sim_src.cpp"
      ],
      "notes": "Removed stale probes into the old single-issue pipeline internals and changed performance accounting to read the current core PerfIF counters through Verilator public-flat hierarchy."
    },
    {
      "scope": "core_compile_fixes",
      "files_modified": [
        "rtl/core/myCPU.sv",
        "rtl/core/PipelineTypes.sv"
      ],
      "notes": "Removed the obsolete cpu_defines.svh include from myCPU.sv. Added the pc and imm payload fields required by the current branch read-reg to execute path."
    },
    {
      "scope": "soc_adapter_previous_step",
      "files_modified": [
        "rtl/soc/student_top.sv",
        "rtl/soc/perip_bridge.sv",
        "rtl/soc/dram_driver.sv"
      ],
      "notes": "Adapted student_top to dual-read IROM core ports. Aligned DRAM return select and byte-offset handling to the address-register plus output-register DRAM timing without adding an extra SoC-side cycle."
    }
  ],
  "verification": [
    "make sim-isa",
    "make sim-coe-correct",
    "make sim-coe-perf"
  ],
  "residual_warnings": "Verilator still reports existing nonfatal RTL warnings such as TIMESCALEMOD, WIDTH, and UNOPTFLAT. These were not fully cleaned in this adapter pass."
}
