# Core RTL Fix Log - 2026-06-01 22:40:34

Scope: only `rtl/core`.

## Fixed

1. Added WriteBack-to-IssueQueue wakeup.
   - Modified `IssueTypes.sv`, `IssueQueueIF.sv`, `IssueQueue.sv`, `WriteBackStage.sv`, `core.sv`.
   - Purpose: wake IQ entries that were dispatched after the producer had already issued but before it wrote back.

2. Added ExecuteMem load request backpressure.
   - Modified `ExecuteMemStage.sv`.
   - Purpose: hold and retry a load uop when `dram.ready` is low, instead of dropping the request.

3. Added Decode serial packet split.
   - Modified `DecodeStage.sv`.
   - Purpose: prevent serial/system instructions from entering Rename in the same packet as another uop.

## Not Changed

- Makefile/filelist is intentionally untouched per current request.
- Full StoreBuffer byte-mask/age forwarding remains deferred as already noted in `doc/core_fix_plan.md`.

## Backup

Original touched RTL files were copied to `backup/20260601_224034/`.
